import 'package:flutter/material.dart';

class Configuration{
  final String name;
  final Color color;
  final Color borderColor;
  final Color fontColor;

  Configuration(this.name, this.color, this.borderColor, this.fontColor);
}